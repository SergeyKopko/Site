from django.apps import AppConfig


class InstitutionPageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'institution_page'
    verbose_name = 'Учреждение образования'
